class RunData {
  final double distance; // Total distance covered during the run (in km)
  final double time; // Total time spent running (in minutes)
  final double avgSpeed; // Average speed during the run (in km/h)

  RunData(this.distance, this.time,
      this.avgSpeed); // Constructor to initialize the run data
}
